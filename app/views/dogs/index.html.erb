<h1>My puppies</h1>
<% @dogs.each do |dog| %>
<div class="card">
<%#= cl_image_tag dog.photo.key, height: 300, width: 400, crop: :fill %>
  <h4>Name: <%= link_to dog.name, dog_path(dog)%></h4>
  <img src="" alt="">
</div>

<% end %>

<%= link_to 'Add a new puppy', new_dog_path %>
